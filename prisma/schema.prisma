generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id        String @id @default(cuid())
  email     String @unique
  fristName String
  lastName  String
  password String
  token sessionToken[]
  tickets sessionTicket[]
}

model sessionToken {
  id Int @id @default(autoincrement())
  token String @unique
  userData User @relation(fields: [userId], references: [id])
  userId String
}

model Movies {
  id Int @id @default(autoincrement())
  movieName String @unique
  decription String
  time String
  price String
  createdAt String 
  reservedSeats Int @default(0)
  Tickets sessionTicket[]
}

model row {
  id Int  @unique @default(autoincrement())
  rowID Int 
  selectedSeats String 
  tickets sessionTicket @relation(fields: [rowID] , references: [ticket])
}

model sessionTicket {
  ticket Int @id @unique
  userData User @relation(fields: [userId] , references: [id]) 
  userId String
  movieData Movies @relation(fields: [movieId] , references: [id]) 
  movieId Int
  useTicket Boolean @default(false)
  rowData row[]
  rowID Int
}